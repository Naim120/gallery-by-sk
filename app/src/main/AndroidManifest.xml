<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Network permissions for Google Drive sync -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Foreground Service permissions (required for WorkManager setForegroundAsync on API 31+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Required on Android 14 (API 34) for typed foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />

    <!-- Storage permissions for Scoped Storage & Direct Deletion -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" tools:ignore="ScopedStorage" />

    <application
        android:name=".GalleryApplication"
        android:allowBackup="true"
        android:largeHeap="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher"
        android:supportsRtl="true"
        android:theme="@style/Theme.GalleryBySK"
        tools:targetApi="34">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ui.viewer.PhotoViewerActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.viewer.VideoEditorActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.vault.VaultSecurityActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK.PrivateSafe"
            android:noHistory="true"
            android:taskAffinity=".PrivateVault"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.vault.PrivateSafeActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK.PrivateSafe"
            android:taskAffinity=".PrivateVault"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|keyboardHidden" />
            
        <activity
            android:name=".ui.vault.VaultSettingsActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK.PrivateSafe"
            android:taskAffinity=".PrivateVault"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|keyboardHidden" />
            
        <activity
            android:name=".ui.vault.PrivateAlbumDetailActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK.PrivateSafe"
            android:taskAffinity=".PrivateVault"
            android:excludeFromRecents="true"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".data.trash.TrashActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity
            android:name=".ui.about.AboutActivity"
            android:exported="false"
            android:theme="@style/Theme.GalleryBySK"
            android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- FileProvider for storage access -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- WorkManager Foreground Service (required for setForegroundAsync on API 31+) -->
        <!-- foregroundServiceType=dataSync required on Android 14 (API 34) -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

    </application>

</manifest>
